node_modules/
dist/
.astro/
.DS_Store
*.log

# Regenerated by scripts/pipeline/run.mjs every run (used for the PR
# body) — not source, never worth committing.
scripts/pipeline/run-summary.md

# Never commit local secrets (e.g. DEEPSEEK_API_KEY for local testing).
.env
.env.*
!.env.example

# Judge scores from a dry run, kept only for local threshold tuning.
scripts/pipeline/run-scores.json
